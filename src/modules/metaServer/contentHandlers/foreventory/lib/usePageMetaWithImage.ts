export const usePageMetaWithImage = async (_path: string): Promise<string | undefined> => {
  try {
    // TODO: Get from puppeteer
    await Promise.resolve()
  } catch (error) {
    console.log(error)
  }
  return undefined
}
